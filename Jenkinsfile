pipeline {
   agent {
          label {
               label "built-in"
               customWorkspace "/mnt/dev"
                 }
         }
          stages {
                   stage ("one") {
                                 steps {
                                          echo "hello"
                                       }
                                 }

                  }

        }
